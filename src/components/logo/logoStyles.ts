import { StyleSheet } from 'react-native';

export const logoStyles = StyleSheet.create({
    view: {
        alignItems: 'center',
        justifyContent: 'center',
        height: '15%',
        marginBottom: 15,
    },
    logo: {
        height: '100%',
        resizeMode: 'contain',
    }
})