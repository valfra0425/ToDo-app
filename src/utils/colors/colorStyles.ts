import { StyleSheet } from 'react-native';

export const colorStyles = StyleSheet.create({
    black: {
        color: '#000'
    },
    white: {
        color: '#fff'
    },
    primaryColor: {
        color: '#fb164d'
    }
})